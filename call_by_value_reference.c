call by value : send a copy of the variable to the function
                the original value does not change
                int x;
                changeValue(a);

call by reference : send an address of the variable to the function
                     the original value can be changed
                     int *x;
                     changeValue(&a);

